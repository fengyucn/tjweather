# 📚 天机气象API工具集 - 文档中心

欢迎使用天机气象API工具集文档！这里包含了完整的使用指南、开发文档和部署说明。

## 📖 快速导航

### 🚀 新手入门
- [安装指南](../INSTALL.md) - 快速安装和配置
- [基本使用](#基本使用) - 5分钟快速上手
- [常见问题](#常见问题) - 疑难解答

### 📖 [用户指南](user-guide/)
- [多要素查询指南](user-guide/多要素查询指南.md) - 一次获取多个气象要素
- [NetCDF数据下载指南](user-guide/NetCDF数据下载指南.md) - 科学数据格式下载

### 🔌 [API文档](api/)
- [天机气象API文档](api/api_readme) - 完整API接口说明
- [错误码参考](api/api_readme#error-codes) - API错误码说明

### 🛠️ [开发文档](development/)
- [命令格式重构报告](development/命令格式重构报告.md) - CLI架构演进
- [CLI工具调试报告](development/CLI工具调试报告.md) - 功能测试报告

### 🚀 [部署文档](deployment/)
- [GitHub设置指南](deployment/GITHUB.md) - 仓库配置和发布
- [仓库初始化报告](deployment/仓库初始化报告.md) - 项目结构记录

## 🚀 基本使用

### 1. 安装工具

```bash
# 克隆项目
git clone https://github.com/yourusername/tjweather.git
cd tjweather

# 运行安装脚本
./tjweather-cli/install.sh
```

### 2. 配置API密钥

```bash
# 初始化配置
tjweather init

# 编辑配置文件
nano ~/.config/tjweather/.env
```

### 3. 开始使用

```bash
# 基本查询
tjweather query -l "116.23128,40.22077" -f t2m -d 3

# 多要素查询
tjweather query -l "116.23128,40.22077" -f "t2m,rh2m" -d 1

# 下载NetCDF文件
tjweather download -l "116.23128,40.22077" -f t2m -d 3 -o weather.nc
```

## 📖 详细指南

### CLI工具使用

#### 查询命令
```bash
tjweather query [选项]
```

**常用选项：**
- `-l, --location <坐标>`: 经纬度坐标
- `-f, --fields <要素>`: 气象要素，多个用逗号分隔
- `-d, --days <天数>`: 预报天数
- `--format <格式>`: 输出格式 (json|table|csv)

#### 下载命令
```bash
tjweather download [选项]
```

**特殊选项：**
- `-o, --output <文件>`: 输出文件路径
- `--filename <名称>`: 指定文件名

### 配置管理

#### 配置优先级
1. 当前目录 `.env`
2. 用户配置 `~/.config/tjweather/.env`
3. 环境变量

#### 配置命令
```bash
# 查看配置
tjweather config

# 显示API密钥
tjweather config --show-secret

# 初始化配置
tjweather init
```

## 🌟 高级功能

### 多要素查询

支持一次查询多个气象要素：

```bash
# 温度和湿度
tjweather query -l "116.23128,40.22077" -f "t2m,rh2m" -d 3

# 更多要素
tjweather query -l "116.23128,40.22077" -f "t2m,rh2m,slp" -d 1
```

### 高分辨率数据

```bash
# 15分钟分辨率
tjweather query -l "116.23128,40.22077" -f t2m -d 0 -h 24 -r 15min

# 高分辨率NetCDF下载
tjweather download -l "116.23128,40.22077" -f t2m -d 0 -h 24 -r 15min
```

### 不同输出格式

```bash
# 表格格式（默认）
tjweather query -l "116.23128,40.22077" -f t2m

# JSON格式
tjweather query -l "116.23128,40.22077" -f t2m --format json

# CSV格式
tjweather query -l "116.23128,40.22077" -f t2m --format csv -o data.csv
```

## 🔌 MCP集成

### 配置MCP服务器

在Claude Code中添加：

```json
{
  "mcpServers": {
    "tjweather": {
      "command": "/path/to/tjweather-mcp/dist/index.js"
    }
  }
}
```

### 可用工具

- **weather_query**: 查询天气数据
- **weather_fields_info**: 获取气象要素信息

## ❓ 常见问题

### Q: 如何获取API密钥？
A: 联系天机气象运营团队获取API授权。

### Q: 支持哪些气象要素？
A: 支持160+气象要素，常用要素包括温度(t2m)、湿度(rh2m)、风速(ws100m)等。

### Q: NetCDF文件如何使用？
A: 可以使用Python(xarray)、R(ncdf4)、MATLAB等工具读取。

### Q: 全局安装失败怎么办？
A: 可以尝试使用`npm install -g .`或检查npm配置。

### Q: 配置文件在哪里？
A: 默认在`~/.config/tjweather/.env`，也可以在项目根目录使用`.env`。

## 🔧 开发者信息

### 技术栈
- **语言**: TypeScript + Node.js
- **CLI框架**: Commander.js
- **HTTP客户端**: Axios
- **MCP协议**: @modelcontextprotocol/sdk

### 项目结构
```
tjweather/
├── tjweather-cli/     # CLI命令行工具
├── tjweather-mcp/     # MCP协议服务器
└── docs/             # 文档目录
```

## 📄 许可证

本项目采用 [MIT 许可证](LICENSE)。

## 🆘 获取帮助

- 📧 邮箱: popfrog@gmail.com
- 🐛 问题反馈: [GitHub Issues](https://github.com/yourusername/tjweather/issues)
- 💬 讨论: [GitHub Discussions](https://github.com/yourusername/tjweather/discussions)

---

**需要更多帮助？** 查看详细的文档目录或联系开发团队。